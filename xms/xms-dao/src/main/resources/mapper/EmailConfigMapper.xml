<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xms.dao.mapper.EmailConfigMapper">

    <resultMap type="EmailConfig" id="EmailConfigResult">
        <result property="id"    column="id"    />
        <result property="email"    column="email"    />
        <result property="status"    column="status"    />
        <result property="enable"    column="enable"    />
        <result property="appAuthPassword"    column="app_auth_password"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateTime"    column="update_time"    />
        <result property="deleted"    column="deleted"    />
    </resultMap>

    <sql id="selectEmailConfigVo">
        select id, email, status, enable, app_auth_password, create_time, update_time, deleted from t_email_config
    </sql>

    <select id="selectEmailConfigList" parameterType="EmailConfig"
            resultMap="EmailConfigResult">
        <include refid="selectEmailConfigVo"/>
        <where>
            <if test="email != null  and email != ''"> and email = #{email}</if>
            <if test="status != null "> and status = #{status}</if>
            <if test="enable != null "> and enable = #{enable}</if>
            <if test="appAuthPassword != null  and appAuthPassword != ''"> and app_auth_password = #{appAuthPassword}</if>
        </where>
        order by id desc
    </select>

    <delete id="deleteRecordById">
        delete from t_email_config where id in
        <foreach item="item" index="index" collection="list"
                 open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>
</mapper>
